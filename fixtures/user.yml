Pimcore\Model\User:
    user{1..2}:
        parentId: '@purchasers_user_folder->id'
        roles: <randomElements(array(@purchaser->id))>
        username: user<current()>
        password: user<current()>
    user{3..4}:
        parentId: '@finance_user_folder->id'
        roles: <randomElements(array(@finance->id))>
        username: user<current()>
        password: user<current()>
    user{5..6}:
        parentId: '@logistics_user_folder->id'
        roles: <randomElements(array(@logistics->id))>
        username: user<current()>
        password: user<current()>
    user{7..8}:
        parentId: '@management_user_folder->id'
        roles: <randomElements(array(@management->id))>
        username: user<current()>
        password: user<current()>
    user{9}:
        parentId: '@content_user_folder->id'
        roles: <randomElements(array(@content->id))>
        username: user<current()>
        password: user<current()>
    apiUser:
        parentId: 0
        username: apiuser
        admin: true
        apiKey: 0417f1505f4802afdfc7e6994f1c904820499eedc335d7f4ce79ac05f8f1f7fc